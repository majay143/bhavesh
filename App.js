
import Users from  './components/Users';
function App() {
  return (
    <Users />
  );
}

export default App;
/* const numbers = [1, 2, 3, 4, 5];

const doubledNumbers = numbers.map((number) => number * 2);

console.log(doubledNumbers); example of array.map method */